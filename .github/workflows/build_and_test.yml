name: Build and Test

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - run: npm ci
      - run: npm run build
  # test:
  #   runs-on: self-hosted

  #   steps:
  #     - run: npm ci
      # - run: npm test
